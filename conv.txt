Psudo code for convolution:


inputs:
- layet to be convoluted (length, width)
- convoluiton size (length, width)

output: 
- convolution of image


Notes to self:
- requires Scipy
- requires json cpp lib ($ sudo apt-get install libjsoncpp-dev)